'use client';

// Lenis smooth scroll is disabled — page scrolling is now controlled
// section-by-section via the custom panel slider in EntranceAnimation.jsx
export default function SmoothScroll({ children }) {
  return <>{children}</>;
}
